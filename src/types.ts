export enum SlideType {
    Straight,
    ShortArc,
    VShape,
    CClockwise,
    Clockwise,
    GrandV,
    PShape,
    QShape,
    PpShape,
    QqShape,
    SShape,
    ZShape,
    Fan
}

export enum UnlanedType {
    Touch,
    TouchHold
}

export enum LanedType {
    Tap,
    Hold,
}